[pytest]
testpaths = tests
pythonpath = .
markers =
    registry: uses the MLflow Model Registry (mlflow.db) instead of the artifacts/*.pkl fallback
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
